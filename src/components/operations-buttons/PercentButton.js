import React, {Component} from 'react';
import './PercentButton.css';

class PercentButton extends Component {
  render() {
    return <button type="button" className="btn btn-gray">%</button>
  }
}

export default PercentButton;